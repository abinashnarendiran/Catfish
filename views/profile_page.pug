extends layout-signedin
block content
  link(rel='stylesheet' href='/bower_components/bootstrap/dist/css/bootstrap.css')
  link(rel='stylesheet' href='/style/birdie.css')
  .container
    #birdie-body
      #eye 
      #wing1
    .row(style='padding-top: 10%;')
      .container.text-center
        h2 Welcome , #{firstName}
    .row(style='padding-top: 60%;') 
      .progress
        .progress-bar.progress-bar-striped.progress-bar-animated(role='progressbar', aria-valuenow='75', aria-valuemin='0', aria-valuemax='100', style='width: 75%')

  script(src='/bower_components/jquery/dist/jquery.js')
  script.
    $(function(){
      $("body").fadeOut(1500,function(){
        window.location.href = "/main"
      })
    });
